#ifndef SOCKETSTREAM_HPP
#define SOCKETSTREAM_HPP

class	SocketStream
{
public:
// constructors & destructor
	SocketStream();
	~SocketStream();
	SocketStream(const SocketStream& socketStream);

// operators
	SocketStream	&operator=(const SocketStream& socketStream);

// member functions
};

#endif
